name: Daily Garner Quant Validation

# Runs Garner Quant every morning at 8am UK time
on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: garner-quant-daily-state
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install requirements
        run: pip install -r requirements.txt

      - name: Validate accounting architecture
        run: |
          python scripts/validate_runtime_state_ownership.py
          python scripts/validate_artifact_hygiene.py
          python scripts/validate_runtime_bootstrap.py
          python scripts/validate_atomic_trade_state.py
          python scripts/validate_execution_input_guards.py
          python scripts/validate_pre_trade_risk_engine.py
          python scripts/validate_risk_shadow_mode.py
          python scripts/validate_canonical_currency_generation.py
          python scripts/validate_transactional_canonical_accounting.py
          python scripts/validate_accounting_observation_envelope.py
          python scripts/validate_accounting_reconciliation.py
          python scripts/validate_accounting_reconciliation_output.py
          python scripts/validate_per_market_bar_scheduler.py
